from matplotlib import pyplot

initial_velocity = 5
gravity = -9.81

x_axis[]
y_axis[]

hight_plot = pyplot

for time_cs in range(0, 200)
    time_s = time_cs*0.01
    height = initial_velocity*time_s + 0.5*gravity*time_x**2
    x_axis.append(time_s)
    y_axis.append(height)

height_plot.plot(x_axis, y_axis
